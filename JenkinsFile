pipeline {
    agent any

    stages {
        stage('Hello') {
            agent {
                docker {
                   image 'node:18-alpine'
                }
            }
            steps {
                echo 'with docker'
                sh 'npm --version'
            }
        }
    }
}
